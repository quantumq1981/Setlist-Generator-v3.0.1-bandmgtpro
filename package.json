{
  "name": "bandleaderhq",
  "version": "4.0.0",
  "private": true,
  "description": "BandLeaderHQ — single-file React app for working bands (setlists, venues, EPK). This package.json exists only to run the pure-algorithm test suite; the app itself has no build step.",
  "scripts": {
    "test": "node --test test/*.test.js"
  }
}
