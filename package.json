{
  "name": "setlist-generator-pro",
  "version": "3.8.0",
  "private": true,
  "description": "Setlist Generator Pro — single-file React app. This package.json exists only to run the pure-algorithm test suite; the app itself has no build step.",
  "scripts": {
    "test": "node --test test/*.test.js"
  }
}
