# Background logo assets

Place the provided background logos in this folder using the following filenames:

- `zemba-logo-bg.webp` (primary modern format)
- `zemba-logo-bg.png` (fallback)

The app's full-screen IDE background uses these paths via CSS `image-set(...)` in `index.html`.
